const nome = "Luiz"; // Criado uma constante chamada nome que armazena uma string.
console.log(nome); // Exibe o valor da constante nome no console.

// Criado uma função chamada somar que recebe dois parâmetros do tipo number.
function somar(a: number, b: number) {
  return a + b; // Retorna a soma dos dois parâmetros.
}

console.log(somar(1, 2)); // Exibe o resultado da função somar no console.
